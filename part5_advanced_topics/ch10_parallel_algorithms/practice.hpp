// Chapter 10: Parallel Algorithms — practice.
//
// Mini-project: Hand-roll a `parallel_accumulate` (chunked across `hardware_concurrency()` threads), then compare its correctness and performance against `std::reduce`/`std::transform_reduce` with `std::execution::par` on the same dataset.
//
// Declare your types/functions here; implement in practice.cpp.

#ifndef CH10_PARALLEL_ALGORITHMS_PRACTICE_HPP
#define CH10_PARALLEL_ALGORITHMS_PRACTICE_HPP

// TODO: declare the mini-project and katas from README.md

#endif // CH10_PARALLEL_ALGORITHMS_PRACTICE_HPP
